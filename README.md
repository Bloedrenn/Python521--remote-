Тестовый проект, ничего дополнительно устанавливать не нужно

## Для работы проекта нужно установить джанго
```bash
pip install django
```

## Подключение React
```bash
npm install --save react react-dom
```